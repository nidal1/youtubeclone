import { View, Text, ScrollView } from 'react-native';
import React from 'react';

export default function tabs() {
  return (
    <View>
      <ScrollView>
        <Text>T</Text>
      </ScrollView>
    </View>
  );
}
